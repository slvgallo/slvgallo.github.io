// Constants extracted from scroll-control.js
const SCROLL = {
  IMG_MAX_HEIGHT: 3.0,
  IMG_MIN_HEIGHT: 2.0,
  HEADER_MAX_PADDING: 5.0,
  HEADER_MIN_PADDING: 1.0,
  SCROLL_RANGE: 200,
  HIDE_DELAY: 3000
};

// Constants extracted from scroll-control.js logic
const SCROLL_BEHAVIOR = {
  SCROLLED_THRESHOLD: 100,
  CONTINUOUS_DOWN_THRESHOLD: 5,
  MOUSE_EDGE_THRESHOLD: 100
};

// Constants extracted from index.js
const INFINITE_SCROLL = {
  ITEMS_PER_LOAD: 16,
  ROOT_MARGIN: "200px"
};

// Constants extracted from scroll-control.js mobile detection
const MOBILE = {
  BREAKPOINT: 768
};

// Constants extracted from scroll-control.js timeouts
const TIMEOUTS = {
  HEADER_SHOW_DELAY: 500,
  HEADER_SHOW_DELAY_HOVER: 300
};
